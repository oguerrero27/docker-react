sudo: required
services: 
  - docker

before_install:
  - docker build -t dindevalhalla/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dindevalhalla/docker-react npm run test -- --coverage

